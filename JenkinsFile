node {
  stage ('Build') {
  echo " Here are the Build Parameter"
  
  sh sudo -s
  sh cd /opt
  sh touch testfile_from_git
  echo "Build complete"
  }
  
  stage ('Deploy') {
  echo "Deploy complete"
  }
  
  stage ('Test') {
  echo "Test complete"
  }
  
}
